---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rolebindwatchers.security.vrungel.maxvk.com
spec:
  group: security.vrungel.maxvk.com
  names:
    kind: RoleBindWatcher
    listKind: RoleBindWatcherList
    plural: rolebindwatchers
    singular: rolebindwatcher
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RoleBindWatcher is the Schema for the rolebindwatchers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleBindWatcherSpec defines the desired state of RoleBindWatcher.
            properties:
              name:
                type: string
              report:
                properties:
                  channel:
                    type: string
                  key:
                    type: string
                  kind:
                    type: string
                type: object
            type: object
          status:
            description: RoleBindWatcherStatus defines the observed state of RoleBindWatcher.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
